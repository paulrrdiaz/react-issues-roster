import styled from "@emotion/styled";
import { Particles as ParticlesComponent } from "react-particles-js";

export const Particles = styled(ParticlesComponent)`
  position: fixed;
  z-index: 0;
  top: 0;
  right: 0;
  left: 0;
  bottom: 0;
`;
